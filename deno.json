{
  "tasks": {
    "run": "deno run --allow-env --allow-net --allow-read --allow-run main.ts",
    "test": "deno test --allow-env --allow-net --allow-read",
    "dev": "deno fmt && deno lint && deno task test --fail-fast"
  }
}
