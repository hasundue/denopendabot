{
  "tasks": {
    "run": "deno run --allow-env --allow-net --allow-read main.ts",
    "test": "deno test --allow-env --allow-net --allow-read",
    "dev": "deno fmt && deno lint && deno task test mod/ integration/mod_test.ts --fail-fast",
    "test-action": "act -j test-action"
  }
}
