name: CI

on:
  pull_request:
    branches: 
      - main
    paths:
      - '**.ts'
      - '**.yml'
  workflow_dispatch:

jobs:
  test:
    name: Test
    uses: ./.github/workflows/test.yml
    secrets: inherit

  deploy:
    name: Deploy
    uses: ./.github/workflows/deploy.yml
    needs: test
    secrets: inherit

  integration:
    name: Integration
    uses: ./.github/workflows/integration.yml
    needs: deploy
    secrets: inherit
